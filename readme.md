.env inside `backend` folder

```
APPLICATION_ID=*
APPLICATION_KEY=*
API_BASE=https://api.schiphol.nl/public-flights
MONGODB_CONNECTION_STRING=mongodb://localhost:27017
```

.env inside `frontend` folder

```
VITE_API_BASE=http://localhost:3050
```

# Running

Backend

cd into the backend folder and then run `npm run build`  and `npm run start`

Frontend


